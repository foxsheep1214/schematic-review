---
name: schematic-review
description: "当用户要审查电路原理图时使用——投板前首审、改版原理图 diff 复审、历史评审意见是否真闭环的核验、核心器件外围电路与 strap/使能/分压参数核实、平台 checklist 逐条核对。典型说法：这版能不能投板、帮我看看原理图有没有问题、评审意见回复说已修改帮我核实一下。输入为 EDA 导出网表（Cadence/OrCAD pstxnet 三件套，或 Altium/KiCad/PADS 等效网表）＋原理图 PDF＋datasheet 包。"
---

# 电路原理图系统审查

> V1.0｜由四个真实工程项目的多轮审查闭环沉淀合并而成，所有规则均有实证出处。

## 核心思想

原理图的电气本质是**网络表**，图形只是可视化。审查不依赖 EDA 软件：把导出网表解析成结构化数据，分两层执行——

- **L0 自动 Lint 层**：机械、无歧义、可穷举的规则全量扫描，解决**覆盖率**（几百网络、几百电源球一个不漏）。Lint 输出是疑似清单，合法结构（Bob-Smith、DNP 选项）人工排除。
- **L1–L6 专家审查层**：链路追踪、供电审计、参数验算、平台规则、读图、器件核实，解决**正确性与风险**。

## 三条铁律

1. **datasheet 原文 > 网表实测 > 页面注释**。注释是"待验证声明"（实例：注释写 V1=20V，实贴电阻算出 18.39V）。
2. **历史评审意见只作线索，必须独立复核**。"回复已修改"不构成闭环证据——逐条对网表验证（假闭环是高频事件）。
3. **每条结论必须有证据链**：refdes + 网络名 + datasheet 条款/页码/表号 + 置信度。没有证据链的发现不进报告。

## 置信度分级（所有结论必须标注）

| 级别 | 含义 | 处置 |
|---|---|---|
| **A 网表实证** | 网表数据直接证明（断网、参数不符） | 可直接整改 |
| **B datasheet 已核** | 经官方 datasheet/规范核实 | 可直接整改 |
| **C 待确认** | 信息不足或来源不可靠 | 明确列出向设计者求证，**严禁臆测** |

器件类别必须 ≥2 个独立证据源才允许定级 A/B（实例：浪涌器件凭库名误判为 TVS，原厂规格书证伪其为 MOSFET 结构件）。

## 严重度分级

| 级别 | 定义 | 处置 |
|---|---|---|
| 致命（BLOCKER） | 上电不工作/损坏器件或上游设备 | 必须修复后投板 |
| 严重（Warning） | 功能失效风险高、违反强制规范、可靠性硬伤 | 必须修复或书面风险接受 |
| 建议（Info/Suggestion） | 提升可靠性/合规性 | 评估后决定 |
| 观察 | 需设计者确认的决策项 | 记录并跟踪 |

## 审查流程（0→10）

```
0. 意图对齐（设计意图问卷）        6. L3 参数与边界验算（WCA）
1. 数据解析（网表→结构化索引）     7. L4 平台/合规规则扫描
2. L0 自动 Lint（规则库全量）      8. L5 图形化复核（读图）
3. L1 关键链路逐条追踪             9. L6 器件与供应链审查
4. L2 供电系统审计                10. 输出分级报告 + 修复-复验闭环
5. L3 前提：datasheet 核实协议
```

### 第 0 步：意图对齐（必须先做）
向设计者索取/确认：板卡功能一句话；电源链路与功耗预算；必须保留/删除的接口清单；环境约束（温度/振动/防护/EMC）；降额策略；不可更改项（认证、绑定芯片、平台强制规则）。**拿不到需求文档时，在报告头部声明"规格符合性判定受限"，涉需求项一律标 C 级"待确认"。**

### 第 1 步：数据解析
Cadence/OrCAD 取 allegro 三件套（pstxnet/pstxprt/pstchip）；其他 EDA 用等效网表，解析规则按格式改写。解析方法、字段含义、参考代码见 `references/netlist-parsing.md`。同时 `pdftotext -layout` 逐页转文本（以页内 File: 字段为准建立实际页面清单，目录页常过期）；扫描件/错乱表格用 `pdftoppm -png -r 200` 渲染目检。

### 第 2 步：L0 全量 Lint
规则库与可运行代码见 `references/lint-rules.md`：单节点悬空网、双胞胎网络名、自动命名孤岛、电源轨/电源球无驱动、VSS 未入地、关键器件计数、参数验算、必需上拉缺失、ESD 挂残网、检测点选错轨、EN 极性/耐压、钳位管直连电源、NC 网络实为真网络、strap 违反强制条款、同名不同域电源轨。DRIVER 前缀等参数按项目调整。

### 第 3 步：L1 关键链路逐条追踪
对意图中的每个"连接对"从 A 逐跳走到 B（网络→元件→网络），断裂点即 bug。必查：电源输入→保护→各级电源→负载；每路数据链路双向；复位链；时钟链；启动配置；检测/反馈链取样点与终点；使能链（谁产生/谁接收/极性/电平域）。**方向敏感信号（TX/RX、差分 P/N）必须回官方引脚语义表逐字确认并做"发射端→接收端"复述，禁止凭感觉交叉。**

### 第 4 步：L2 供电审计
每轨三问：谁驱动（注意 0R 跳线选项，须追另一端找真实源）/谁负载/电平对不对。SoC 全部电源球有驱动、全部 VSS 球入 GND。电平域匹配：列出各 IO 域实际供电，核对相连两端域电平一致。每轨功率预算留 ≥20-30% 裕量。反灌审计：上拉不得向未上电器件倒电。热与浪涌：高压分压电阻功耗、开关器件 I²R 耗散、热插拔浪涌 I=C·dV/dt。

### 第 5 步：L3 参数与边界验算（WCA）
公式库见 `references/wca-formulas.md`（FB 分压、UVLO/OVLO、限流、软启动、钳位、ADC 分压、RC 复位、I2C 上拉、MLCC 偏压、电阻功耗）。代入网表**实际阻容值**（E96 实值，不是标称意图）复算。每个关键电路过边界清单：最高/最低输入、满载/空载、启动/热插拔、短路、温度范围、公差叠加。选型复核含**封装兼容性**（电气可换≠物理可焊）。

### 第 6 步：L4 平台/合规规则扫描
把平台官方 checklist/设计指南中可判定条目转成检查表逐条核对。通用域检查表（电源/时钟/复位/接口/防护/监控/无源/连接器/文档，适用于任何板卡）见 `references/review-checklist.md`；平台专属规则库按项目自行构建，构建方法与空模板见 `references/methodology-v1.0.md` 第 7 章。

### 第 7 步：L5 图形化复核
网表覆盖不了的信息在图形里：渲染关键页读图（新增页全读），目检清单——二极管/TVS 阴极方向、LED/电解电容极性、连接器 pin1、变压器同名端、Option/NC 贴装表、strap 配置表。**多封装同页引脚图必须按标题逐张对应，禁止跨图引用**。比对 PDF 与网表的导出时间，防止拿旧图审新版。

### 第 8 步：L6 器件与供应链
新 IC 介入第一步先读 Absolute Maximum Ratings（实例：EN 脚耐压 5.5V 被上拉 24V）。核实公式常量（Vref、限流/频率公式）后再出参数。查不到 datasheet 的标 C 级并索取，严禁凭印象补参数。供应链：生命周期、单一来源、替代可行性；网络聚合页信息一律不可靠。

### 第 9 步：输出报告
骨架与发现项格式见 `references/report-template.md`。要点：准出判定表（致命=0/BOM 二义/书面确认/ERC/封装核对/Layout 约束逐项判定）；发现项 ECO 格式（位置/证据/根因/改法/验证）；历史意见闭环三档（真闭环/假闭环/待确认）；已验证通过清单（带证据留痕）；更正说明（对前轮错误结论逐条更正，不悄悄改）；Layout 约束传递；计算留档附录。

### 第 10 步：修复-复验闭环
修复后重新导出网表 → 复跑 L0 + 修复项专项验证 → 与上版 diff（BOM 变更+网络变更），确认"只改了该改的" → 致命/严重清零后输出终审结论与投板前目检清单。改版复审沿用同一框架：先跑新旧网表全量 diff，再对前轮问题逐条做闭环验证（实例：前轮 22 项问题表格化逐条判定 已修复/未闭环/未变）。

## 高频错误模式库（每轮强制自查）

- [ ] 网络名分裂（差下划线/后缀，同一轨两个名字互不相通）
- [ ] "NC" 被当成网络标号：所有"不用引脚"被短接成一张大网（OrCAD 经典陷阱）
- [ ] 多封装同页引脚图跨图误引（先确认实物封装再看对应图）
- [ ] "参照 demo 板"违反 datasheet 强制条款（must be pulled down / must be left floating / 默认态）
- [ ] 假闭环：以"已修改"回复代替网表验证
- [ ] 库符号引脚映射错（新建/定制符号必查；沿用成熟库可免检）
- [ ] 器件身份与外围参数不匹配（库名/封装/分压三者对不上）
- [ ] 检测点接错侧（判压接在保护后/负载侧）
- [ ] ESD 挂残网（防护在空网，活线裸奔）
- [ ] EN 脚超耐压/极性反（低有效上拉、高有效下拉）
- [ ] 钳位管/稳压管直接跨接超压轨
- [ ] 上拉电源域与信号域不一致/向上电未完的器件倒灌
- [ ] strap 采样电平被同节点 LED/上拉拖偏（复位采样窗口算分压）
- [ ] 同名不同域电源轨张冠李戴（VCC_3V3 ≠ VCC_3V3_SOM）
- [ ] 串联下臂被当成单电阻（分压比算错主因）
- [ ] 可调阈值只按 typ 算不给 min/max 范围
- [ ] 估算值当 datasheet 原文引用（功耗/电流数字标明来源）
- [ ] 网络命名与电平/功能不符误导后来者
- [ ] 扫描件/损坏文本层 datasheet 未渲染目检就下结论
- [ ] 把目录页当实际页面清单（以页内 File: 字段为准）

## 工作纪律

- 结论可被质疑：每条发现给出可复现核验路径（文件:行号/页码/表号）；被反驳时用证据复核，错了明确撤回并归档教训
- 更正前轮（含自己）结论写成"更正说明"，不悄悄改
- 报告写入项目目录；中间产物（页面文本、渲染图、底稿）放 /tmp；交付物只有报告本身
- 本 skill 的 references/ 与 examples/ 均为可执行资产，审查时按需取用，不必全量加载：
  - `references/methodology-v1.0.md`：方法论完整长文档（含 L0~L6 各章细节、平台规则库构建方法与模板、其他 AI 平台迁移指南）
  - `references/netlist-parsing.md` / `lint-rules.md` / `review-checklist.md` / `wca-formulas.md` / `report-template.md`：解析、规则库、通用检查表、验算公式、报告模板
  - `examples/`：报告范例，写报告时参照其详略与格式
    - `worked-example-industrial-gateway.md`：一份完整的合成范例报告（虚构板卡），涵盖准出判定表、BLOCKER/Warning/Info 分级、闭环三档、误报排除、计算留档等全部骨架
