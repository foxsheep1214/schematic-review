# 审查报告模板

交付物只有报告本身，写入项目目录；中间产物（页面文本、渲染图、底稿）放 /tmp。编号体系在头部说明（如 E-xx=致命、W-xx=严重、S-xx=建议；或 P-xx=初审、F-xx=复核），便于跨轮追溯。

## 报告骨架

```markdown
# <项目名> 原理图审查报告（首审/复审/合并版）

- 审查对象：<原理图版本、页数、网表导出时间、板卡一句话>
- 审查依据：<datasheet 清单（关键器件必须全量阅读）、平台 checklist 版本、历史评审记录>
- 审查方法：<网表全量解析（器件/网络数）+ 逐页审查 + datasheet 复算 + L0~L6>
- 对比基线：<复审时填：上版网表 + 上轮报告编号范围>
- 日期 / 限制说明：<无需求文档时声明"规格符合性判定受限">

## 一、总体结论
<一段话：架构是否正确、致命几项、严重几项、能否投板>

### Layout 准出判定
| 准出条件 | 结果 |
|---|---|
| 致命问题 = 0 | 满足/不满足（编号列表） |
| BOM 二义清零 | 满足/不满足（型号二义逐项列出） |
| 规格偏离书面确认 | 待办清单 |
| ERC/DRC 清零 | 单引脚死网络数；OrCAD DRC 待办 |
| 封装核对 | 已抽核项；投板前必做项 |
| Layout 约束传递 | 已具备/需补齐 |

## 二、BLOCKER（投板前必须清零）
<每条按发现项格式>

## 三、Warning（需书面处置结论）

## 四、Info / Suggestion
<表格：# / 位置 / 问题 / 建议>

## 五、历史意见闭环总表（复审/有历史记录时必备）
- 真闭环：<编号 + 网表证据>
- 假闭环（重新打开）：<编号 + 声称已改但网表未变的证据>
- 待确认：<等什么（FAE/需求/原厂）>

## 六、已验证通过的关键项（带证据留痕）
<抽样列出，防止下轮重复劳动；同样写证据>

## 七、PCB Layout 必须携带的约束
<大电流载流/RF 50Ω/差分阻抗与等长/隔离爬电/去耦就近/热/测试点可触探>

## 八、更正说明（对前轮报告的修正）
<逐条：原结论 → 更正后结论 → 依据>

## 附录：关键计算留档
<所有复算：公式 + 实际阻值（位号）+ 代入 + 结果（typ/范围）+ 判定基准>
```

## 发现项格式（可直接当 ECO 单执行）

```
[ID] [级别] 标题
位置：位号/网络/页码
证据：网表节点数据或 datasheet 条目（置信度 A/B/C）
根因：为什么会错
改法：具体到位号、阻值、网络名的修改动作
验证：改完后如何确认（网表复扫/bench 实测点）
```

示例（合格发现项）：

```
[E-05] [BLOCKER] <器件> TEST[3:0] 全部实装上拉，违反 datasheet 强制条款
位置：05 页 Uxxx，Ryy1~Ryy4 → 3V3（strap 实配 = 1111）
证据：datasheet Table N 原文 "TEST[3:0]: Reserved for internal use. Must be pulled down."（B 级）；
      网表确认四颗上拉实装、同位下拉 Rxx1~Rxx4 全部 /NC（A 级）
根因：参照 demo 板上拉，demo 做法不能凌驾 datasheet 强制条款
改法：上拉四颗改 /NC，同位下拉 Rxx1~Rxx4 改实装（纯 BOM 改动，不动板）
验证：重出网表后逐一确认 TEST[3:0] 各节点实装电阻的另一端为 GND
```

## 写作要求

- 严重度词与处置绑定：BLOCKER=投板前清零；Warning=书面处置结论；Info=评估后决定；观察=记录跟踪
- 每个 BLOCKER/Warning 给可复现证据链（文件:行号/页码/表号）
- 修正前轮结论必须显式写"更正说明"，不悄悄改
- 被质疑后复核：用证据复核，错了明确撤回、归档教训、更新报告三处（发现项/准出判定/闭环表）
- 准出判定最后必须能落成一句话结论（如"F-05/F-07 清零 + 三项料号唯一化 + P-01 取得功耗数据前不得投板"）
